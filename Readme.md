# Heart Disease Prediction

This project predicts heart disease using a Random Forest model.

## Features
- Machine Learning pipeline with preprocessing
- Flask API for predictions
- Accuracy ~98%

## How to run

1. Install dependencies:
pip install -r requirements.txt

2. Run Flask app:
python app.py

3. Send POST request to /predict